/**
 * See all documentation on https://github.com/johnagan/clean-webpack-plugin
 *
 * @type {{root: *, verbose: boolean, dry: boolean, watch: boolean, exclude: Array}}
 */

module.exports = {

    // Write logs to console.
    verbose: true,

    // Remove files on recompile.
    watch: true,

};